before_script:

# EzMake defaults to building and running all tests
test_all:
  script:
    - make init
    - make build MODE=dynamic
    - make test
    - make clean
    - make init
    - make build MODE=dynamic
    - make test
